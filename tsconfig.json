/// "es6" is the library(version of javascript) that ts will use.  Default is es3
// "dom" is javascript web api interfaces.  console.log will error in .ts files without this

{
  "compilerOptions": {
      "lib": ["es6", "dom"],
      "strict": true
    }
}
